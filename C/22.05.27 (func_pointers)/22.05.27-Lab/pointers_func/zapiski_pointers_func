Kakwo predstawlqwat arguments на main()
- void - не приема аргументи 
- – int main(argc, *argv[ ]) - те се подават 
    argc - броя на променливите, от обкръжението, което ние имаме на комплютъра
    *argv (масив от указатели) - стрингове, които съдържат самите променливи на обкръжението

    УКАЗАТЕЛ КЪМ ФУНКЦИЯ

1. Указва изпълним код 
2. Подобно на указателите към данни, можем да разгледаме указател към машинен код

int myfunc(double, int); /* prototype function */
void fnPointerDemo() {
 int (*ptrfun) (double, int);
 ptrfun = &myfunc;
 ptrfun = myfunc;
 int nValue = (*ptrfun) (1.0, 1);
}
int mystery(int a, int b, int (*fn)(int,int)) {
 return ((*fn)(a,b));
} 

myfunc - В С самото име на тази функция представлява един указзател към фция. 
Това е начален адрес от който се стартира една функция. 

int (*ptrfun) (double, int); - указател към фунцкия, 
(double, int)-типовете, които приема тази функция като аргумент. 
Най отляво int е return type  т.е. това което връща фцията. 
(*ptrfun) - самия указател

През този указател ние можем да си извикаме самата фунцкия. Този указател сочи точни към определ вид функция, 
кочто приема точки определен тип данни (double, int) и връща int. 

